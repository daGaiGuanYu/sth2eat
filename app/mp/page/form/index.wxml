<form class="full-width">
  <view class="input-group">
    <input class="row" placeholder="饭单名称输在这里" />
  </view>

  <view class="input-group">
    <view class="input" wx:for="{{list}}" wx:key="index" data-index="{{index}}" bindtap="handleTapItem">
      <text wx:if="{{item.name}}">{{item.name}}</text>
      <text wx:else class="placeholder">请输入饭/店名</text>
      <text class="arrow">></text>
    </view>
  </view>

  <button bindtap="addItem">添加菜品</button>
  <button type="primary" bindtap="submit">提 交</button>
</form>