page {
  --margin: .9em;
}
.full-width {
  margin-bottom: 2em;
  --border: .2px solid #eee;
}

.row-group {
  padding: 0 var(--margin);
  border-bottom: var(--border);
  border-top: var(--border);
  margin-bottom: var(--margin);
  background: #fff;
}

.under-group {
  color: #aaa;
  margin: .6rem 1rem 0.3rem;
  font-size: .8rem;
}

.full-width .row {
  padding: var(--margin) 0;
}
.full-width .row:not(:last-child) {
  border-bottom: var(--border);
}

.full-width .row.label {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.full-width .placeholder {
  color: #888;
}

.full-width .row .arrow {
  font-size: 1.1em;
  transform: scale(1, 1.3);
  font-weight: 300;
  color: #aaa;
}

button.row, .full-width button {
  margin: calc(var(--margin) / 1.5) var(--margin);
}
.bottom-btn-group {
  position: fixed;
  bottom: 2em;
}
button.row {
  width: calc(100vw - calc(2 * var(--margin)));
}